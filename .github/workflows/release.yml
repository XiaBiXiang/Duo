name: Build & Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.29.3'
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK (split per ABI)
        run: flutter build apk --release --split-per-abi --dart-define=GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Rename APKs
        run: |
          VERSION=${{ steps.version.outputs.VERSION }}
          cd build/app/outputs/flutter-apk
          mv app-arm64-v8a-release.apk "Duo-${VERSION}-arm64-v8a-release.apk"
          mv app-armeabi-v7a-release.apk "Duo-${VERSION}-armeabi-v7a-release.apk"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: Duo ${{ steps.version.outputs.VERSION }}
          body: |
            ## Duo ${{ steps.version.outputs.VERSION }}
            ### 更新内容
            - 请查看下方提交记录

            ### 下载
            - `Duo-${{ steps.version.outputs.VERSION }}-arm64-v8a-release.apk` — 适用于大多数现代 Android 手机
            - `Duo-${{ steps.version.outputs.VERSION }}-armeabi-v7a-release.apk` — 适用于旧款 Android 手机
          files: |
            build/app/outputs/flutter-apk/Duo-${{ steps.version.outputs.VERSION }}-arm64-v8a-release.apk
            build/app/outputs/flutter-apk/Duo-${{ steps.version.outputs.VERSION }}-armeabi-v7a-release.apk
          draft: false
          prerelease: false
